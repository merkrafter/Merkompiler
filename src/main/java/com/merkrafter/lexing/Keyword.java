package com.merkrafter.lexing;

/****
 * This enum lists all keywords that can be encountered in JavaSST files.
 *
 * @since v0.2.0
 * @author merkrafter
 ***************************************************************/
public enum Keyword {
  CLASS,
  ELSE,
  FINAL,
  IF,
  INT,
  PUBLIC,
  RETURN,
  VOID,
  WHILE,
}
